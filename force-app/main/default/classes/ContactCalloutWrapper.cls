public class ContactCalloutWrapper {
    public String id;
    public Boolean is_active;

    public ContactCalloutWrapper(String id, Boolean is_active) {
        this.id = id;
        this.is_active = is_active;
    }
}